# 全球博物馆展览设计案例库｜智能体复刻执行说明

## 1. 项目使命

维护并扩展网站 **“展陈田野｜全球博物馆展览设计案例库”**。这是一个以展览设计、策展叙事、参观动线、展项与布展为中心的研究型案例库，而不是博物馆建筑、旅游导览或艺术展览索引。网站以中文呈现，并为每个已收录案例提供真实落地展厅照片、五层分析、来源链接和可下载的独立 PDF。

当前工程是**纯静态 React 网站**。不得为了采集、搜索、文件管理或定时更新引入后端、数据库、认证、tRPC、爬虫服务或后台任务。案例数据与媒体路径均在构建时固定，详情页与筛选在前端运行。

> 核心质量原则：宁可暂缓候选，也不能用不完整责任链、建筑图片、效果图、旅游照、低质量照片、单一设备供应商资料或无运营证据的项目凑数。

| 项目维度 | 已确认要求 |
|---|---|
| 收录主题 | 中大型叙事性主题展、常设展或长期展；优先社会史、城市史、战争／记忆、人权、科学、自然史、文化遗产、产业／企业社会史等 |
| 地理范围 | 全球；优先补足未充分覆盖国家／地区，曾重点要求中文地区、港澳台、东南亚、美洲、南美、东欧、西欧、日本、韩国、俄罗斯、高加索、加勒比、东非等 |
| 排除对象 | 纯艺术／美术展、短小临展、仅建筑改造、单个装置、单设备供应商项目、未落地概念、责任链或室内实景不完整项目 |
| 单批规模 | **精确 5 案**；未凑齐 5 个完整合格案例，禁止启动媒体、PDF、前端接入或发布 |
| 当前发布基线 | 第34批已发布：334个案例、464张真实项目图；后续批次从已发布版本继续 |

## 2. 候选发现与“爬取”策略

本项目的“爬取”不是无差别下载网页或图片，而是**以设计责任证据为中心的定向研究与审计**。优先从展陈设计公司、展览设计工作室、制作方或设计—制作联合体的项目页发现候选，再只在必要时用馆方页面补强当前运营、展览叙事与公共开放信息。每个候选原则上控制在“设计方页 + 馆方页”两条核心来源内；如两页仍无法闭合必要事实，立即暂缓，不作多轮泛搜。

所有网页和媒体只作被动获取与研究性索引。遵守来源网站的访问限制、使用条款和署名要求；不得绕过登录、付费墙、反爬限制或访问控制。图片在网页与 PDF 中保留原始来源／摄影署名（公开时）和研究性使用说明，不制造或伪称版权授权。

### 2.1 候选优先级

先检查现有 `client/src/data/` 是否已有同一博物馆、同一主展或同一改版版本。没有重复后，按以下顺序筛选：具名整体展陈责任最完整者优先；仍开放的长期叙事展优先；具两张原始已建成内景且能呈现不同章节者优先；新国家／地区优先。设计方项目页若只描述建筑、品牌空间或单一设备，不能视为整体展陈责任。

| 证据类别 | 合格证据 | 不合格或不足证据 |
|---|---|---|
| 整体责任 | 设计方明确写明 exhibition design、masterplan、interpretive design、content design、design-build、fabrication，或馆方清晰署名策展／展陈领导 | 仅建筑师、照明、AV设备、数字屏、家具或单件装置供应商；未说明职责的媒体转载 |
| 运营状态 | 馆方当前“visit/opening hours/exhibitions”页面，或可靠的当期运营页面 | 多年前开幕新闻、旧活动页、关闭公告、无法确认当前状态的社媒 |
| 叙事结构 | 馆方／设计方说明章节、主题、面积、展项、参观路径或核心命题 | 只有馆名、室内美图或抽象营销文案 |
| 图像 | 原始项目页、馆方、摄影师公开发布的真实已建成**室内展厅**摄影 | 效果图、概念图、外观、施工图、旅游自拍、活动合影、单件展品特写、无法判断的室内图 |

### 2.2 每案严格准入条件

一个案例只有在下列五项同时成立时，才能在候选台账标记为 `accepted-for-5-case-pool`：

1. **展览边界明确**：必须是中大型、有清晰叙事边界的常设或长期主题展，不把整座博物馆建筑当作一个案例。
2. **责任闭合**：有可追溯的整体展陈设计／制作责任；若没有设计方，馆方明确的策展与展陈责任可作为替代，但必须写清责任主体。
3. **当前运营闭合**：有可靠证据显示仍在开放、正常运营或作为长期展持续提供参观。
4. **叙事闭合**：能够明确说出展览解释的核心问题与主要章节／时间线／主题线，不能只给题名。
5. **双图闭合**：至少两张原始来源的真实已建成室内图，人工视觉确认后须对应不同叙事章节、空间或观看关系；每张图要保存 URL、来源、摄影署名（如有）、场景和审核结论。

发现任何一项缺失时，在台账以 `deferred`、`excluded` 或 `duplicate` 记录，并写明具体缺口。不要在之后的媒体、PDF或前端流程中使用该候选。

### 2.3 候选台账要求

每一批建立唯一 JSON 台账，如 `research/expansion_1000_batchNN_candidates.json`。台账至少要记录下表字段；状态是所有后续生产的唯一准入依据。

| 字段 | 用途 |
|---|---|
| `id`、`title`、`museum`、`location`、`year`、`type`、`region` | 固定案例身份与前端筛选标签 |
| `status`、`admission_reason`、`defer_reason` | 放行、暂缓、排除、重复的可审计结论 |
| `design_source`、`design_source_secondary`、`fabrication_source` | 整体设计／制作责任证据与角色边界 |
| `museum_source`、`operations_source` | 馆方叙事和当前开放／运营证据 |
| `image_source_1`、`image_source_2` | 两张候选内景图的原始 URL |
| 图片审核记录 | 图1／图2的场景、真实性、是否已建成、是否不同章节、来源／摄影署名、人工审核结论 |
| `duplicate_check` | 对 `client/src/data/` 的ID、馆名、展名与版本排重结果 |

批次结束前建立一个确定性验证脚本。它必须验证：台账中恰有5条 `accepted-for-5-case-pool`；5个 ID 唯一；必填证据字段不为空；每案两张图已冻结；所有 ID 不与现有 `caseLibrary` 重复。未通过不得进入生产。

## 3. 低成本、但不降质量的批次流水线

严格采用“**先闭池，后生产；每类动作只做一次；批末一次发布**”的批处理方式。避免逐案上传、逐案写 PDF、逐案构建、频繁截图和中间发布。

| 阶段 | 何时执行 | 最少动作 | 必留产物 | 禁止事项 |
|---|---|---|---|---|
| A. 候选审计 | 每找到候选即记录 | 设计方优先 + 必要时馆方补强；一次人工双图审查 | 唯一候选JSON台账 | 未满5案前下载、上传、写PDF、改前端 |
| B. 5案门禁 | 恰满5案后 | 运行台账／ID／字段／双图门禁 | 验证脚本与通过记录 | 用低标准项目补满数量 |
| C. 媒体 | B通过后 | 一次批量下载10图、规范化、上传 | 下载冻结清单、最终媒体清单、上传清单、一次视觉复核 | 重复下载／上传同一图；把图放进项目源码目录 |
| D. 内容与PDF | C完成后 | 复用模板统一生成5套内容和Typst项目 | 内容JSON、每案Typst源、构建计划、验证JSON、PDF视觉复核 | 逐案边写边发布；把推演平面说成原图 |
| E. 静态接入 | D全部通过后 | 新增一批数据模块、测试与一次聚合更新 | 数据模块、批次测试、历史尾部偏移更新 | 反转源数组；引入后端或数据库 |
| F. 批末门禁与发布 | E完成后 | 完整测试与代表性浏览器回归一次 | 构建结果、回归记录、单一检查点 | 中间检查点、重复构建、单案发布 |

## 4. 图片获取、审核、规范化与上传

每批只处理通过台账门禁的 **10 张图**。下载前生成 `batchNN_declared_media_downloads.json`，冻结每张图片的案例 ID、编号、原始 URL、来源／摄影署名、对应叙事段和审核结论。下载到项目目录外的 `/home/ubuntu/webdev-static-assets/`；项目内的 `client/public/` 与 `client/src/assets/` 禁止存放大图或PDF。

图片规范化脚本只能读取冻结的原图，输出外置 JPEG，并用 Pillow 等工具重新打开验证文件有效性。建立 `batchNN_final_media_manifest.json`，列出输出路径、尺寸、来源和场景。随后批量上传一次，建立 `batchNN_uploaded_image_manifest.json`；网页与 PDF 都只能引用上传回传的 `/manus-storage/...` 持久化路径。

人工视觉审核要求：确认图像不是渲染或建筑外观；图像内有可辨识的展厅、展柜、图文、展项、投影、对象或观众观看关系；同一案例两图不应是同一机位的重复；两图尽可能体现不同叙事段。复核后以联系表或逐对检查形式记录到 `batchNN_media_visual_review.md`。只做一次最终审核；没有新增信息时不重复截图或审阅。

## 5. 五层展陈分析与独立PDF

每案须从公开、已核验来源生成五层分析。不要讨论建筑造型、工程结构、预算或未公开技术参数；不要把根据照片推导的动线写成官方平面。研究判断必须使用条件性表述，并明确是案例库的研究性分析。

| 固定层级 | 写作内容 |
|---|---|
| 平面与动线 | 入口、定向、章节门槛、主／支路径、停留与出口的**研究性**参观序列；若无官方平面，明确不是施工／布展原图 |
| 策展叙事 | 核心命题、章节关系、时间／主题／人物／问题线的认知转折 |
| 展项设计 | 关键对象、模型、互动、影像、声音、档案或场景如何组织观众动作、信息和情绪 |
| 布展与图文 | 展柜、对象密度、灯光、材料、色彩、图形、导视、观看距离与低刺激替代路径 |
| 研究判断 | 可迁移的方法、适用前提和不能外推的场地／馆藏／预算依赖；严格区分事实与判断 |

每案输出独立 Typst 报告，至少 **5页**、至少 **2张已审核项目图**，应包括：项目证据页、动线、叙事章节、展项设计、布展系统、研究判断、来源及图片权利信息。使用现成批次主题与生成模板，不为无新增信息反复改版。推荐固定环境：Typst 0.15.1、`Noto Serif CJK SC` 与 `Noto Sans CJK SC`。

每案从项目根目录编译：

```bash
typst compile --root /home/ubuntu main.typ main.pdf
```

随后使用现有严格验证工具进行 image-bearing 验证：

```bash
verify_pdf.py main.pdf --profile image-bearing --min-pages 5 --min-images 2 --strict --report main.verify.json
```

通过后上传5份PDF并建立 `batchNN_uploaded_pdf_manifest.json`。PDF视觉审核只需对每案一次，确认文本未溢出、两张实景图存在、图文页可读、来源页存在；记录在 `batchNN_pdf_visual_review.md`。

## 6. React静态接入规范

保持既有技术栈：React 19、TypeScript、Vite、Tailwind 4、Wouter、Framer Motion、Vitest。只能接入静态数据，不使用任何服务端数据请求。

每批新建 `client/src/data/caseExpansion1000BatchNN.ts`，输出5个符合 `CaseItem` 的对象；图片和PDF使用批次上传manifest中的 `/manus-storage/...` 路径。修改 `client/src/data/caseLibrary.ts`：导入新批次模块，并将批次数组**追加到源数组末尾**。不要反转源数组，因为详情页“上一篇／下一篇”依赖旧→新的数组顺序；首页显示层已负责最新优先。

| 页面行为 | 不可回退的要求 |
|---|---|
| 首页 | 新案例显示在上方；案例卡和真实项目图都可点击进入详情；保留筛选、检索与渐进加载 |
| 详情 | 显示两张真实项目图、五层分析、PDF入口、来源链接；底部可访问上一篇／下一篇 |
| 移动端 | 详情页有返回主页入口，并保留滑动／手势返回体验；文字、图片和PDF入口可读可点 |
| 地区筛选 | 新案只出现在正确地区；不得出现跨区残留；北欧、西欧、南欧、中欧等标签必须准确 |

新增 `caseExpansion1000BatchNN.test.ts`，至少断言：数量为5、ID唯一、每案双图／PDF皆为 `/manus-storage/`、五层分析完整、源数组尾部顺序正确。同步更新受影响的历史尾部 `slice` 断言。

## 7. 必经验证、清理与发布

批次准备发布时，按以下顺序只运行一次完整门禁：

```bash
pnpm test
pnpm run verify:case-ids
pnpm run check
pnpm run build
```

浏览器回归只覆盖受影响路径：首页最新案例置顶；一个图片直接进入详情；至少一个桌面详情的双图、PDF和相邻导航；一个移动详情的返回；一个受影响地区筛选。检查本轮网络／控制台日志，没有新图片或PDF加载错误才可发布。不要为所有历史案例重复做截图测试。

发布前只清理**可再生**文件：每案 `main.pdf`、`main-review/`、批次联系表渲染缓存和临时上传日志。必须保留 Typst 源、`case.json`、内容manifest、构建计划、严格验证JSON、候选台账、媒体／PDF上传清单、内容JSON和视觉复核Markdown。之后在 `research/todo.md` 标记当前批完成，并保存**唯一一个**检查点；该项目开启自动发布，保存检查点就是正式发布。

不得向用户宣称尚未保存检查点的候选、临时图片或开发预览内容已经完成／发布。每个完整批次发布后只汇报实际可见成果、案例总数、图片总数、PDF数量、关键验证结论和新版本链接。

## 8. 可直接粘贴给其他智能体的执行指令

```text
你负责维护“展陈田野｜全球博物馆展览设计案例库”，一个纯静态 React/TypeScript/Vite 网站。请严格执行以下规则：

1. 目标是收录全球中大型、叙事性、常设或长期主题展的展陈设计案例；优先社会史、记忆、人权、战争、科学、自然史、文化遗产及产业社会史。排除纯艺术展、小型临展、建筑改造、单设备项目和概念图项目。
2. 每批必须精确5案。未凑齐5个完整合格案例之前，只更新唯一候选审计台账；严禁下载／上传媒体、写PDF、改前端、构建或发布。
3. 每案同时满足：明确整体展陈设计／制作责任或明确馆方策展责任；当前开放／可靠运营；清晰叙事；两张人工审核的真实已建成室内展厅图；原始来源。任一缺失即deferred/excluded/duplicate并写明理由，绝不凑数。
4. 发现候选优先使用设计方项目页；仅在需要补足开放和叙事时加一个馆方页。先对现有client/src/data排重。避免重复泛搜，遵守来源网站限制。
5. 台账记录案例身份、状态、责任来源、运营来源、叙事来源、两张图的原始URL／摄影署名／场景／人工审核、重复检查和准入理由。用确定性脚本验证恰5案、ID唯一、字段完整、双图完整且不与全库重复。
6. 闭池后才一次性处理10图：下载至/home/ubuntu/webdev-static-assets，冻结下载清单，规范化为JPEG并验证可打开，人工复核每案双图，再一次上传。网页和PDF只能引用上传回传的/manus-storage路径；不得把媒体放进client/public或client/src/assets。
7. 每案写五层分析：平面与动线、策展叙事、展项设计、布展与图文、研究判断。只写已公开可核验事实；没有官方平面时必须标“研究性推演，非原始平面”。
8. 每案生成独立Typst PDF，至少5页、至少2张实景图，含来源与图片权利信息。根目录编译，之后以image-bearing严格验证（≥5页、≥2图）并做一次可读性视觉复核。上传5份PDF，记录持久化路径。
9. 静态接入时新建一个批次数据模块与批次测试。将新批数组追加到caseLibrary源数组末尾，绝不反转源数组；首页显示层已负责新内容置顶。确保图片可直接进详情、详情有PDF与上下篇、移动端可回主页。
10. 批末按固定顺序运行pnpm test、pnpm run verify:case-ids、pnpm run check、pnpm run build。只做首页最新案例、一个桌面详情、一个移动详情、图片/PDF入口、相邻导航和受影响地区筛选的代表性浏览器回归。
11. 清理仅可再生main.pdf、渲染review目录、联系表缓存和临时上传日志；保留源、manifest、case.json、验证JSON、台账和复核记录。更新TODO后只保存一个检查点并发布。不得引入后端、数据库、认证、tRPC、定时任务或后台爬虫。
12. 始终采用低成本工作流：单来源优先、逐案一次证据闭合、5案闭池后单次媒体／内容／PDF／前端批处理、批末一次全量门禁、单一发布。只报告已经实际完成并发布的结果。
```

## 9. 交接文件定位

| 文件／目录 | 交接用途 |
|---|---|
| `client/src/data/caseLibrary.ts` | 全库聚合、旧→新顺序和新批接入位置 |
| `client/src/data/cases.ts` | `CaseItem` 数据结构与五层字段 |
| `research/expansion_1000_batch34_candidates.json` | 最近一个完整候选台账范例 |
| `research/verify_batch34_pool.mjs` | 精确5案、字段、双图和全库排重门禁范例 |
| `research/assemble_batch34_final_media.py` | 批量媒体规范化和重开验证范例 |
| `research/build_batch34_case_content.mjs` | 五层分析内容生成范例 |
| `research/build_batch34_pdf_sources.mjs` | 五案Typst源项目批量生成范例 |
| `research/build_batch34_static_data.mjs` | 从台账、内容与上传清单生成静态数据范例 |
| `research/expansion_1000_batch34_pdfs/` | 独立PDF源、构建计划与严格验证结果范例 |
| `research/low_cost_quality_workflow.md` | 精简执行策略补充说明 |
| `research/todo.md` | 长期历史；只新增／完成当前批相关项，不倒回执行旧遗留项 |

